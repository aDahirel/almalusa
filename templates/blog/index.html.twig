{% extends 'base.html.twig' %}
{% block body %}
    <style>
        body{
            background-color: #f5f5f5;
        }
    </style>
    <!-- Articles section -->
    <section class="container py-5">
        <!-- Display all the articles in database -->
        {% for article in articles %}
            <article class="mb-5 d-flex flex-column justify-content-between">
                <!-- Article title -->
                <h2>{{ article.title }}</h2>
                <!-- Article picture -->
                <img src="{{ vich_uploader_asset(article, 'imageFile') }}" alt="article picture" style="max-width: 250px">
                {% if article.content | length > 200 ? article.content %}
                    {% set contentSliced = article.content | slice(0, 100) ~ ' ...' %}
                    <span> {{ contentSliced | raw }} </span>
                {% else %}
                    <span> {{ article.content | raw }} </span>
                {% endif %}
                <!-- Article date -->
                <div class="metadata font-weight-bold">Libellés :
                    {% for wording in article.wordings %}
                        <li class="list-unstyled">
                            <a href="" style="color:#212529;">
                                {{ wording.title }}
                            </a>
                        </li>
                    {% endfor %}
                </div>
                <!-- Article page button -->
                <a href="{{ path('blog_show', {'id' : article.id, 'slug': article.slug }) }}"
                    class="btn btn-secondary">En savoir plus</a>
            </article>
        {% endfor %}
        <div class="btn-group">{{ knp_pagination_render(articles) }}</div>
    </section>
    <!-- / Articles section -->
{% endblock %}