{% extends "base.html.twig" %}
{% block title %}Se connecter{% endblock %}
{% block body %}
    <head>
        <link rel="stylesheet" href="{{ asset('css/login.css') }}">
    </head>
    <div class="text-center d-flex vh-100" style="background-color: #f5f5f5;">
        <!-- Connexion form -->
        <form action="{{ path('security_login') }}" method="post" class="form-signin mx-auto w-100 p-3">
            <img src="{{ asset('images/alma_lusa_logo.png') }}" height="100px" alt="Logo" class="mb-4">
            <h1 class="h3 mb-3" style="color: black;">Se connecter</h1>
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <div class="form-group">
                <input placeholder="Adresse email" value="{{ last_username }}" required name="_username" type="text"
                       class="form-control input1">
                <input placeholder="Mot de passe" required name="_password" type="password" class="form-control input2">
            </div>
            <div class="custom-control custom-checkbox my-1 mr-sm-2 mb-3">
                <input type="checkbox" class="custom-control-input" id="remember_me" name="_remember_me">
                <label class="custom-control-label" for="remember_me">Se souvenir de moi</label>
            </div>
            <div class="buttons">
                <div class="form-group">
                    <button type="submit" class="btn btn-lg btn-success w-100">Connexion</button>
                </div>
            </div>
            <p><a href="{{ path('security_registration') }}">S'inscrire sur Alma Lusa</a> ·
                <a href="{{ path('home') }}"> Mot de passe oublié ?</a></p>
            <p><a href="{{ path('home') }}">Retour</a></p>
        </form>
    </div>
{% endblock %}
